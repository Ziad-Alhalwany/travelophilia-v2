<div dir="rtl">

# Report: TP-BE1-002-001

| Field | Details |
| --- | --- |
| **Task ID** | TP-BE1-002 |
| **Agent** | BE1 |
| **Date** | 2026-03-14 |
| **Model** | Gemini |
| **Scope / Allowed Paths** | `backend/django_api/trip_requests/serializers.py`, `docs/api.md` |

## Summary
- تمت مراجعة جميع ملفات الـ Views والـ Serializers الخاصة بتطبيقي `trips` و `trip_requests`.
- تم التأكد من تغطية جميع الـ Endpoints المطلوبة لعرض الرحلات وتسجيل الـ Leads من الموقع.
- تم تطبيق (Defensive Programming) بصرامة داخل `TripRequestCreateSerializer` لحقول (travelers و children_details) عبر التأكد من أنها `List of dicts` لمنع أخطاء إدخال البيانات النصية.
- تم تحديث العقد المركزي `docs/api.md` ليكون المصدر الأساسي للـ Frontend، بالاعتماد الكلي على نمط `snake_case` مع جميع المسارات، المدخلات، والمخرجات (Request/Response JSON).

## Proposed Commit
**Files changed:**
1. `backend/django_api/trip_requests/serializers.py`
2. `docs/api.md`

**Proposed Commit Message:**
```text
feat(be1): TP-BE1-002 - Add strict JSONField validation and update API contract

- Implemented defensive programming in `TripRequestCreateSerializer` for `travelers` and `children_details` fields.
- Overhauled `docs/api.md` to define standard JSON Responses using `snake_case`.
(agent:be1)
```

## Risks
- الاعتماد على `public_code` في الـ GET التفصيلي للرحلات قد يعود برسالة 404 إذا تم إدخال slug أو كود غير مسجل في قاعدة البيانات. 
- يجب على الـ Frontend التأكد من إرسال البيانات بـ `snake_case` كما يملي ملف `docs/api.md` رغم وجود دمج للحقول القديمة بـ `camelCase` مؤقتاً في الـ Serializer.
- لا توجد مخاطر كبيرة متوقعة نظراً لإبقاء باقي الـ Endpoints بدون تعديلات جذرية في الـ logic.

## Next Steps
- يرجى من فريق الـ `FE2` تحديث الـ Converters/Mappers لديهم لمطابقة العقد المحدث في `docs/api.md`.
- يرجى من فريق الـ `Doc Agent` تحديث ودمج العقد الجديد `api.md` إذا استدعى الأمر.
- إجراء اختبارات تكامل (Integration Tests / Smoke Tests) لضمان نجاح إنشاء طلب رحلة (TripRequest) جديد من الـ Frontend.

## Example (مثال تطبيقي)
**قبل التعديل:**
غياب حماية الـ `JSONField` للمسافرين كان سيؤدي لتمرير سلسلة نصية عادية:
`"travelers": "Ahmed and his brother"`

**بعد التعديل (Defensive Programming):**
سيعيد الـ API خطأ 400 Bad Request مباشرة:
```json
{
  "travelers": ["Must be a list of objects."]
}
```

</div>
