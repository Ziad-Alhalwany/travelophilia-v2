# TP-FE2-001 Chat Summary

- **What was discussed:** The necessity of mapping frontend `camelCase` code to the backend `snake_case` API contract smoothly, and implementing automated JWT rotation for the unified Axios client.
- **Decisions made:** Integrated `toSnakeDeep` and `toCamelDeep` directly into Axios interceptors (`src/services/apiClient.js`). Created a sophisticated 401 interceptor loop testing for SimpleJWT tokens inside local storage using `authStorage.js`, falling back dynamically. 
- **Open questions:** Does `crmAuth.js` endpoints overlap with the primary `apiClient.js`? Currently kept separated as requested by scopes.
- **Related report:** `TP-FE2-001-001.md`
