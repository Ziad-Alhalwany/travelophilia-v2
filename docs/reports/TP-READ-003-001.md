# Report: TP-READ-003-001 (Governance Refinement)

- **Agent**: Release
- **Date**: 2026-01-19
- **Scope**: `docs/identifiers.md`, `docs/locks.md`, `docs/tasks.md`
- **Proposed Commit Message**: `NO-COMMIT (docs): update governance (identifiers/locks/tasks) with strict rules (agent:release)`

## 1) Goal

Refine governance documents to match final engineering and operational decisions regarding Codes, Locking, and Task Tracking.

## 2) Changes Implemented

### A) `docs/identifiers.md`

- **Logic**: Defined `Trip Public Code` (Immutable) vs `Slug` (Mutable).
- **Format**:
  - Customer: `ST-xxxx-Rxxxx-Pxx`
  - CRM: `ST-xxxx-Rxxxx-Pxx-L-xxxx`
- **Engineering**: Mandated dual-lookup support (Public Code + Slug) in Backend.

### B) `docs/locks.md`

- **Table**: Cleared placeholders.
- **Enums**: `ACTIVE`, `RELEASED`, `STALE`, `WAITING`.
- **Rule**: Shared docs (`tasks`, `locks`, etc.) are protected; only modifiable via Release Agent.

### C) `docs/tasks.md`

- **Structure**: Added **Priority** column (`P0`, `P1`, `P2`).
- **Rule**: "Done" status requires specific Proof (Handoff/Reference).

## 3) How to Test (Verification)

- **Identifiers**: Review `docs/identifiers.md` for the "Glossary" and "Agent Checklist".
- **Locks**: Check `docs/locks.md` contains the new Enums section.
- **Tasks**: Check `docs/tasks.md` has the `Priority` header and the "Done Rule" block.

## 4) Risks

- **Identifier Complexity**: Developers might skip the "Dual Lookup" check if not enforced in Code Review.
- **Lock Bottleneck**: Restricting shared docs to "Release Agent Only" might slow down updates if I am not active. (Mitigation: Self-signing allowed if authorized).

## 5) Unauthorized Touches?

- **None**. Only files within `docs/` were modified. No `src/` or `readme` touches.
